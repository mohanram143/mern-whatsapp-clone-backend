<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>IT Asset Handover Report - Awal Plastics</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: Arial, sans-serif; font-size: 14px; color: #333; background: #fff; }

    /* ── HEADER ── */ 
    .header { padding: 24px 40px 16px; }
    .header-top { display: flex; justify-content: space-between; align-items: center; }
    .header-company h2 { font-size: 20px; font-weight: 700; margin-bottom: 2px; }
    .header-company .sub { color: #777; font-size: 13px; }
    .header-logo { height: 70px; }
    .header-divider { border: none; border-top: 1px solid #ddd; margin: 16px 0 14px; }
    .header-meta table { font-size: 13px; border-spacing: 0; }
    .header-meta td { padding: 2px 6px 2px 0; }
    .header-meta td:first-child { font-weight: 700; width: 110px; }
    

    /* ── EMPLOYEE INFO ── */
    .section { padding: 0 40px; margin-bottom: 24px; }
    .section-title { font-size: 15px; font-weight: 700; margin-bottom: 10px; }
    .section-divider { border: none; border-top: 1px solid #ddd; margin-bottom: 14px; }
    .emp-table { width: 100%; font-size: 14px; border-spacing: 0; }
    .emp-table td { padding: 4px 6px; vertical-align: top; }
    .emp-table td:first-child { width: 180px; color: #555; }
    .emp-table td:nth-child(2) { width: 20px; color: #555; }

    /* ── EXECUTIVE SUMMARY ── */
    .summary-box { background: #edf7ee; border-radius: 6px; padding: 16px 20px; margin-bottom: 16px; display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px; }
    .summary-box h3 { font-size: 14px; font-weight: 700; margin-bottom: 4px; }
    .summary-box p { font-size: 13px; color: #555; }
    .summary-stats { display: flex; gap: 30px; }
    .stat { text-align: center; }
    .stat .val { font-size: 22px; font-weight: 700; color: #2e7fd8; }
    .stat .lbl { font-size: 11px; color: #666; margin-top: 2px; }

    /* ── NOTES BOX ── */
    .notes-box { background: #faf4e8; border: 1px solid #f0d18a; border-radius: 6px; padding: 16px 20px; margin-bottom: 20px; }
    .notes-box h3 { font-size: 14px; font-weight: 700; margin-bottom: 10px; }
    .notes-box ul { padding-left: 18px; }
    .notes-box ul li { font-size: 13px; margin-bottom: 5px; color: #555; }

    /* ── TABLES ── */
    .data-table { width: 100%; border-collapse: collapse; margin-top: 8px; }
    .data-table th { background: #f5f5f5; padding: 10px 12px; border: 1px solid #ddd; text-align: left; font-size: 13px; font-weight: 700; }
    .data-table td { padding: 10px 12px; border: 1px solid #ddd; font-size: 13px; vertical-align: middle; }
    .data-table tr:nth-child(even) td { background: #fafafa; }

    /* ── STATUS BADGES ── */
    .badge { display: inline-block; padding: 3px 10px; border-radius: 12px; font-size: 11px; font-weight: 700; border: 1px solid; }
    .badge-revoked    { background: #e8f5e9; color: #2e7d32; border-color: #a5d6a7; }
    .badge-disabled   { background: #fdecea; color: #c62828; border-color: #ef9a9a; }
    .badge-suspended  { background: #fff3e0; color: #e65100; border-color: #ffcc80; }
    .badge-deactivated{ background: #fff8e1; color: #f57f17; border-color: #ffe082; }
    .badge-removed    { background: #fce4ec; color: #880e4f; border-color: #f48fb1; }
    .badge-cleared    { background: #e8f5e9; color: #2e7d32; border-color: #a5d6a7; }

    /* ── CHECKLIST ── */
    .checklist-table { width: 100%; border-collapse: collapse; margin-top: 8px; }
    .checklist-table td { padding: 10px 12px; border: 1px solid #ddd; font-size: 13px; }
    .checklist-table tr:nth-child(even) td { background: #fafafa; }
    .check-icon { width: 20px; height: 20px; background: #1565c0; border-radius: 3px; display: inline-flex; align-items: center; justify-content: center; }
    .check-icon::after { content: '✓'; color: #fff; font-size: 13px; font-weight: 700; }

    /* ── APPROVALS ── */
    .approvals-grid { display: flex; gap: 16px; flex-wrap: wrap; margin-top: 12px; }
    .approval-card { flex: 1; min-width: 220px; border: 1px solid #ddd; border-radius: 6px; overflow: hidden; }
    .approval-card-header { padding: 10px 16px; text-align: center; font-size: 13px; font-weight: 700; color: #fff; }
    .approval-card-header.requested { background: #1565c0; }
    .approval-card-header.approved  { background: #1565c0; }
    .approval-card-header.rejected  { background: #1565c0; }
    .approval-card-body { padding: 14px 16px; font-size: 13px; }
    .approval-card-body .role { color: #777; font-size: 12px; margin-bottom: 4px; }
    .approval-card-body .name { font-weight: 700; margin-bottom: 8px; display: flex; align-items: center; gap: 8px; }
    .avatar { width: 28px; height: 28px; border-radius: 50%; background: #bdbdbd; display: inline-block; }
    .approval-date { font-size: 12px; color: #666; display: flex; align-items: center; gap: 4px; }
    .seen-row { margin-top: 10px; font-size: 12px; color: #777; display: flex; align-items: center; gap: 6px; }
    .seen-avatars { display: flex; }
    .seen-avatars span { width: 22px; height: 22px; border-radius: 50%; background: #bdbdbd; display: inline-block; margin-left: -6px; border: 2px solid #fff; }
    .seen-avatars span:first-child { margin-left: 0; }
    .seen-count { background: #1565c0; color: #fff; border-radius: 50%; font-size: 10px; padding: 2px 5px; }

    /* ── DIGITAL VERIFICATION ── */
    .digital-box { border: 1px solid #90caf9; border-radius: 6px; padding: 16px 20px; margin-top: 20px; }
    .digital-box h4 { font-size: 14px; font-weight: 700; margin-bottom: 8px; }
    .digital-box p { font-size: 12px; color: #555; word-break: break-all; }

    /* ── COMPLIANCE ── */
    .compliance-section { padding: 0 40px; margin-bottom: 28px; }
    .compliance-title { font-size: 15px; font-weight: 700; color: #1565c0; border-bottom: 2px solid #1565c0; padding-bottom: 6px; margin-bottom: 16px; }
    .compliance-item { margin-bottom: 14px; }
    .compliance-item h4 { font-size: 13px; font-weight: 700; margin-bottom: 4px; }
    .compliance-item p { font-size: 13px; color: #555; line-height: 1.6; }

    /* ── FOOTER ── */
    .footer { padding: 20px 40px; border-top: 1px solid #ddd; margin-top: 10px; }
    .footer-inner { display: flex; justify-content: space-between; align-items: flex-start; flex-wrap: wrap; gap: 16px; }
    .footer-left { font-size: 12px; line-height: 1.8; color: #444; }
    .footer-left strong { display: block; margin-bottom: 2px; }
    .footer-center { font-size: 12px; line-height: 1.8; color: #444; direction: rtl; text-align: right; }
    .footer-right { font-size: 12px; line-height: 1.9; color: #444; }
    .footer-right a { color: #1565c0; text-decoration: none; }
    .footer-logos { display: flex; flex-direction: column; align-items: flex-end; gap: 6px; }
    .cert-badges { display: flex; gap: 6px; flex-wrap: wrap; align-items: center; }
    .cert-badge { border: 1px solid #aaa; border-radius: 4px; padding: 3px 7px; font-size: 10px; color: #555; font-weight: 700; }
    .print-btn { display: block; text-align: right; padding: 10px 40px 20px; }
    .print-btn button { background: #1565c0; color: #fff; border: none; padding: 9px 22px; border-radius: 5px; font-size: 14px; cursor: pointer; }
    .print-btn button:hover { background: #0d47a1; }

    @media print {
      .print-btn { display: none; }
    }
  </style>
</head>
<body>

<!-- ══════════════════════════════════════════
     HEADER
══════════════════════════════════════════ -->
<div class="header">
  <div class="header-top">
    <div class="header-company">
      <h2>Awal Plastics</h2>
      <div class="sub">IT Asset Handover Report</div>
    </div>
    <!-- Replace src with actual logo path if needed -->
 <img
              src="https://storage-ui.colanapps.in/files/awal-plastics-images/awalpastic-logo-update.svg"
              style="width: 180px; padding-right: 8px"
            /> </div>
  <hr class="header-divider" />
  <div class="header-meta display:fle">
    <table>
      <tr><td><b>Report ID</b></td><td>: EX-2025-001-RPT</td></tr>
      <tr><td><b>Generated</b></td><td>: June 9, 2025 at 2:45 PM</td></tr>
      <tr><td><b>Generated By</b></td><td>: IT Assets Management System</td></tr>
      
    </table>
    
  </div>
</div>

